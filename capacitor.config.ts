import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.legalhelpkolkata.app',
  appName: 'Legal Help Kolkata',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
